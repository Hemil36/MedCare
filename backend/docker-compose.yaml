version: "3.9"

services:
  backend:
    build: ./
    container_name: medcare-backend
    restart: always
    ports:
      - "5000:3000"
    env_file:
      - .env


#   mongo:
#     image: mongo:6
#     container_name: medcare-db
#     restart: always
#     ports:
#       - "27017:27017"
#     volumes:
#       - mongo_data:/data/db

# volumes:
#   mongo_data:
